import { FC } from 'react';

import { Header, CalculatorForm } from '../../components';

export const CalculatorPage: FC = () => {
  return (
    <div>
      <Header />
      <CalculatorForm />
    </div>
  );
};
